name: shell commands

on: [push]

jobs:
    run-shell commands
    runs-on: ubuntu-latest
    steps:
        -name: checkout
            uses: actions/checkout@v3
        -name: update
            uses: sudo apt update
        -name: multiline script
            run: |
            node -v
            pwd
